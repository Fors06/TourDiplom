<Window x:Class="Diplom.View.Admin.The_common_window.Windows.AddBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diplom.View.Admin.The_common_window.Windows"
        mc:Ignorable="d"
        Title="Добавление бронирования" Height="450" Width="800">
    <Grid  Style="{DynamicResource GridBackgroundStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Выберите клиента:" Grid.Row="0" Grid.Column="0"/>
        <ComboBox Name="ClientsComboBox" ItemsSource="{Binding Clients}" DisplayMemberPath="ФИО" SelectedValuePath="КлиентId" Grid.Row="0" Grid.Column="1"/>

        <Label Content="Выберите путешествие:" Grid.Row="1" Grid.Column="0"/>
        <ComboBox Name="ToursComboBox" ItemsSource="{Binding Tours}" DisplayMemberPath="Название" SelectedValuePath="ПутешествиеId" Grid.Row="1" Grid.Column="1"/>

        <Label Content="Дата начала:" Grid.Row="2" Grid.Column="0"/>
        <DatePicker Name="StartDatePicker" SelectedDate="{Binding StartDate}" Grid.Row="2" Grid.Column="1"/>

        <Label Content="Дата окончания:" Grid.Row="3" Grid.Column="0"/>
        <DatePicker Name="EndDatePicker" SelectedDate="{Binding EndDate}" Grid.Row="3" Grid.Column="1"/>

        <Button Content="Добавить" Command="{Binding AddBookingCommand}" Grid.Row="4" Grid.Column="1" Margin="5"/>
    </Grid>
</Window>